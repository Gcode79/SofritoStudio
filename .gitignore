# Secrets — NEVER commit
.env
config/.env
*.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
cloudflare/node_modules/

# Product source (large binaries — PDFs, videos) — keep out of git
course/
packages/

# OS
.DS_Store
Thumbs.db

# Generated / local
*.log
*.pid

# Deploy archive backups
*.zip

# Cloudflare wrangler build artifacts
cloudflare/.wrangler/
deploy/.wrangler/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# Retail worker config at repo root (deploy hazard) — never commit
/wrangler.jsonc

# Local content plan queue (generated drafts stay on the PC)
pivot-site/content/queue/

# Legacy pre-pivot tree (kept on PC, not in git)
deploy/
cloudflare/
mailer/
legacy-webhook-server/
starter/
gumroad-uploads/
social_automation/
docs/
PACKAGE-MANIFEST.md
PDF-PRODUCT-STATUS.md
DEPLOYMENT.md
EMAIL-AUTOMATION.md
README.md
fix_product_pages.py
sofrito-example/

# Legacy workflows (moved to SofritoStudio-Legacy)
.github/workflows/blog-amp.yml
.github/workflows/marketing-daily.yml
.github/workflows/package-sequences.yml
.github/workflows/post-purchase-followup.yml
.github/workflows/reengagement.yml
.github/workflows/welcome.yml
