# Ops — Decision Log (standing + open)

> Log discipline: every executed decision records what, when, why (evidence), who. Open decisions list options + recommendation + owner status. Sign-off rule from business-model.md is always in force: no publish/deploy/spend/contact/pricing without owner approval.

## Executed decisions (proxy-executed with owner sign-off standing)
| # | When | Decision | Evidence | Status |
|---|---|---|---|---|
| D1 | 2026-09-08 | Sofrito Session = 90 min @ $400 (was 75 min); written action-plan deliverable; $267/hr effective, mid-band for established-consultant tier | pricing benchmark research; site localization | LIVE (copy deployed, verified) |
| D2 | 2026-09-08 | Payment processor = Stripe for sessions/services/projects (vs Gumroad 10%+fee): B2B invoice expectation, services not goods, ~$11.90 vs ~$52 all-in at $400 | cost comparison; tax-comfort analysis | LIVE (Gumroad DORMANT, legacy only) |
| D3 | 2026-09-08 | Sessions route via contact-form pipeline until Calendly URL provided; KV `site/config.session_url` swap-in ready | current booking dependency | LIVE (no URL yet) |
| D4 | 2026-09-08→09 | Booking automation (S19): `/api/calendly-webhook` w/ signature verify (t.v1 HMAC, 5-min tolerance), invitee.created/canceled → bookings + lead mirror + owner emails + webhooks; billing gated on KV `config/booking_billing` (default `'none'`) | staged; migration applied to remote D1 | **STAGED, NOT DEPLOYED** |
| D5 | 2026-09-08 | Data hygiene: probe/test lead removed from remote D1 | verified absent | DONE |
| D6 | 2026-09-09 | Lead-pool re-audit: all 15 D1 rows verified as QA/deploy artifacts (09-05 pipeline tests; 09-08 Zapier/publish/deploy tests + founder self-test). Prior "11 real leads" premise **RETRACTED** — no contact targets exist in D1. OD1 reframed to a demand-source decision. | full 15-row D1 `leads` pull + name/source/timestamp cluster analysis | DONE (all docs patched) |
| D7 | 2026-09-09 | Repo-scoped operating system installed per founder choice: `opencode.json` (default_agent=operator; edit/bash/task ask; external_directory deny; todowrite allow) + `.opencode/agents/` (operator/researcher/strategist/growth/builder/reviewer/deployer) + BUSINESS REASONING PROTOCOL into repo-root `AGENTS.md` (now the standing instruction set) | founder selection "Repo-scoped (SofritoStudio)"; customize-opencode skill schema | DONE — activates after **opencode restart** |
| D8 | 2026-09-09 | Installed dedicated **Social Content Operator** subagent (`.opencode/agents/social.md` per founder-supplied system): drafting/planning only, zero publish/account/DM/ad access, every public item Draft + Awaiting Founder Approval, publishing gated post-by-post via a separate (future) agent; supports ONLY existing food-business studio services | founder-supplied system prompt + launch command | DONE — activates after **opencode restart** |
| D9 | 2026-09-09 | **GA4 consent-gated snippet (gap A5)** in pivot-site: add `loadGA4()` to `public/js/site.js` using legacy GA4 ID `G-7XFBM9JMEV`, gated on `consentGiven()` / `ss_consent==='1'` so no gtag loads before opt-in (matches privacy.html claim); fires automatic page_view on consent and on boot for returning consenting visitors; `anonymize_ip` on | legacy `consent.js` port (verified GA_ID `G-7XFBM9JMEV`, consented pattern); pivot `consentGiven()` = `'1'` (verified); founder-selected gap; `node --check` passes | `VERIFIED` in source — **NOT DEPLOYED (owner approval required for deploy)** |
| D10 | 2026-09-09 | **Stripe pipeline verification (todo 2):** prod `sofrito-studio` holds only 5 secrets (ADMIN_KEY, BUTTONDOWN_API_KEY, RESEND_API_KEY, TIKTOK_EVENTS_TOKEN, WEBHOOK_URL). **`STRIPE_WEBHOOK_SECRET` and `STRIPE_API_KEY` are NOT deployed** → invoice-trigger records milestones but never sends a Stripe invoice (worker L671 warning path), and any real Stripe webhook POST returns `missing signature` 400 (L738-741), so no Stripe revenue can land in D1. Webhook endpoint (`/api/stripe-webhook` + `invoice.paid`/`checkout.session.completed`/`charge.refunded`) must be configured in the Stripe dashboard. `CALENDLY_WEBHOOK_SIGNING_KEY` also absent (matches OD3 blocker) | Cloudflare Workers API secrets read (primary, names only, no values); worker `src/worker.js` L544-585/L648-671/L737-821; `wrangler secret list` returned `[]` twice — CLI anomaly, API authoritative | **VERIFIED FINDING** — Stripe pipeline INERT until founder sets both secrets + dashboard webhook; setting secrets = owner approval |
| D11 | 2026-09-09 | **`page_view` emission (gap F2)** in pivot-site: add `track('page_view', { referrer: document.referrer || null })` to `public/js/site.js` — called in the onReady boot (L408-410, fires for returning consenting visitors) and in the consent-ok accept branch (L175-182, first-load visitors right after opt-in). Single-fire guaranteed: `track()` no-ops unless `consentGiven() === '1'`, and `bindConsent` early-returns when consent already stored, so no page can emit twice | analytics contract `starter/docs/analytics.md` defines `page_view` (never emitted); site.js `track()` L40-54 is consent-gated (verified); both call sites verified; `node --check` passes | `VERIFIED` in source — **NOT DEPLOYED (owner approval required for deploy)** |
| D12 | 2026-09-09 | **guide_url 301 (gap F3)** VERIFIED — no code change needed: newsletter handler returns `guide_url = SITE_URL + '/freebies/digital-guide.md'` (worker L410); `public/_redirects` L42 `/free* → /contact.html 301` catches it (bytes verified this session); smoke-test `scripts/smoke-test-apis.js` L28 asserts `guide_url` contains `digital-guide`; live curl earlier confirmed digital-guide.md → 301 /contact.html. Chain = intended lead-magnet capture. Nuance (no action): `/guide` exact → `/blog/brand-story.html` (L13) while wildcard `/guide*` → `/contact.html` (L44) | worker L410; `_redirects` L42/L13/L44; smoke-test L28; live curl | `VERIFIED` — requirement already satisfied |
| D13 | 2026-09-09 | **env-drift README cross-check (gap F4)** FIXED: pivot-site README secrets block corrected to match `.env.example` — removed vestigial `GUMROAD_WEBHOOK_SECRET`, replaced `MAKE_WEBHOOK_URL` with `WEBHOOK_URL`, added `TIKTOK_EVENTS_TOKEN`, `STRIPE_API_KEY`, `STRIPE_WEBHOOK_SECRET`, `CALENDLY_WEBHOOK_SIGNING_KEY`, and a pointer that `.env.example` is the authoritative list (local-only vars like `OPENROUTER_API_KEY` stay in `.env`, not worker secrets). Stack table payments row now "Stripe (services + projects; Gumroad dormant)" per D2; "Any purchase (Gumroad/Stripe webhook)" → Stripe events only | D10 deployed-secrets list (API); `.env.example` L29-45; README L18/L38-46/L78 | `VERIFIED` — doc corrected (internal edit, no deploy) |
| D14 | 2026-09-09 | **day-14 check-in (gap F6)** = header-comment drift, NOT a missing deployed email: worker header (L26-28) claimed a "day-14 check-in" on the schedule, but `runLeadAutomations` sends only drip day 2/5/9 (DRIP_PLAN L1085-1089), the 24h owner reminder (day≥1, L1127), and day-1/day-30 post-delivery follow-ups for won leads (L1140-1162); no `follow-up-day14.html` exists in `src/emails/`. Header comment corrected to match shipped behavior. Removing a day-14 nurture email from the plan is logged as OD8 (owner call when real leads exist) | worker L26-28/L1085-1089/L1102-1165; `src/emails/` glob (no day14 file); observed drift vs D6 (zero real leads) | `VERIFIED` — header corrected (internal doc edit, no deploy) |
| D15 | 2026-09-09 | **Money truncation (gap F7) FIXED in code (not deployed):** revenue views used SQLite integer division (`SUM(amount_cents)/100` where `amount_cents` is INTEGER) so cents were truncated to whole dollars (e.g. 25045 → 250, dropping 45¢). Corrected to `/100.0` in `schema.sql` L135/L137/L147 (`v_pipeline`, `v_monthly_revenue`), recreated the views in new migration `0005_fix_money_views.sql` (drop + rebuild, immutable `0001` left pristine and superseded), fixed the admin inline query (`src/worker.js` L515), and replaced the digest's hardcoded `.00` with `(Number(x)||0).toFixed(2)` (L1179-1180). Remaining `/100` money sites: none (only `0001_initial.sql`, superseded). `node --check` passes. Impact is owner-facing only (v_pipeline → weekly digest; v_monthly_revenue → dashboard/admin) and latent until real revenue exists | schema.sql L135/137/147; `migrations/0005_fix_money_views.sql`; worker L515/L1179-1180; grep site-wide `/100`; `node --check` SYNTAX_OK | `VERIFIED` — code-complete, **NOT DEPLOYED (owner approval required for migration 0005 + worker deploy)** |
| D16 | 2026-09-09 | **Remotion deliverable = branded in-repo skill pack** (`pivot-site/skills/`, README + 6 `remotion-*` skills): founder's final instruction "go with the recommended option" supersedes the earlier "just a mapping doc" answer. Pack adapts the global `remotion-*` skills to AGENTS.md brand rules (tokens, formats 9:16/4:5/1:1, Path A deterministic text, Realistic Social Visual Standard, `DRAFT — AWAITING FOUNDER APPROVAL` gate). Legacy `remotion-abuelas-ipad` Starter Kit + "Abuela's iPad" ad content excluded by directive; non-vendored global skills (docs/interactivity/maps/markup/multimedia/saas) load globally when needed. No project/ad content in the pack — instructions only | founder choice (final message); `pivot-site/skills/` glob (7 files present); global skills inventory (12 `remotion-*`) | DONE — pack created (internal files, no deploy). Registration: junction into `pivot-site/.agents/skills/` per README (not yet linked) |

## Open decisions (owner — each blocks a step)
| # | Decision | Options | Recommendation | Status |
|---|---|---|---|---|
| OD1 | Real-demand source (was "triage the 11 leads"; pool = QA, no targets) | (a) pursue social-scan enrichment prospects (DavIsa/El Inquieto, Growee, El Chilar HF, Paldy) — the only real people identified; (b) capture-validation move only (fix leaks, measure real traffic); (c) hold | (a) after the 3 leak fixes — evidence-led targeting | awaiting owner |
| OD2 | Retainer waitlist capture | (a) POST /api/leads capture, (b) keep inert | (a) — pure win, reversible | awaiting owner |
| OD3 | Session checkout | (a) deploy staged Calendly+Stripe bundle, (b) stopgap copy now + defer | (a) if inputs arrive in 30d, else (b) immediately | awaiting owner inputs (4) + go |
| OD4 | Brand Foundation (G3) | price / fold into Sofrito / drop | price in $1k–$1.5k band **after** pricing research | awaiting owner + research |
| OD5 | La Mesa policy | referral-only scarcity vs open volume | scarcity + referral incentive | awaiting owner |
| OD6 | Tier 3 digital products | approve / defer / kill | defer until first client wins; then revisit | awaiting owner |
| OD7 | Billing mode for bookings | charge-at-booking vs invoice-after | invoice-after for trust at first; revisit at scale | awaiting owner |
| OD8 | Day-14 nurture email (documented in worker header, never implemented) | (a) implement (new template + DRIP_PLAN step; founder-approved copy + deploy), (b) skip — drip 2/5/9 + day-1/30 suffices | (b) for now — zero real leads (D6); revisit when pipeline has real targets | awaiting owner |

## Required inputs awaiting owner (blocker detail for OD3)
1. Calendly 90-min event URL
2. Calendly webhook signing key
3. Stripe account + `STRIPE_API_KEY` secret
4. Billing mode (OD7)

## Standing rules (not decisions — constraints)
- Research gate: every decision 100% research-based; content best-practices gate before anything commits.
- Pivot confinement: no resurrection of the old $47-product/retail model.
- Sign-off before: deploys, commits (of consequence), pricing, launches, external billing, campaign spend, external contact.
- Data hygiene: record invoices/payments in D1 as they occur; never fabricate numbers.